#include <iostream>

// Creating a constant
#define X 10;

int main() {
    // Creating a constnt
    const int x = 20;
    // We can never change the value of x

    // Another way
    enum {y=30};
}