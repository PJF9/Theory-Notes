ScrollViewExample:

<ScrollViewExample>:

    StackLayoutExample:
        # If we want to scroll verticaly we set 1, None else None 1
        size_hint: 1, None
        height: self.minimum_height # This is the size of the size of the widnow.
