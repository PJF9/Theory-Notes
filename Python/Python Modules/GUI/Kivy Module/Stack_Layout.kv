StackLayoutExample:

<StackLayoutExample>
    # Changing the orientation of the stack layout
    # left-right, top-bottom
    # orientation: "lr-tb"
    orientation: "lr-bt"

    # Adding padding to the stack layout, from top-left -> top-right -> bottom-right -> bottom-left
    padding: ("20dp", "20dp", "20dp", "20dp")

    # Adding different spacing
    spacing: "1dp", "10dp"

    Button:
        text: "Hallo"
        size_hint: .2, .2

    Button:
        text: "There"
        size_hint: .2, .2

    Button:
        text: "Sir"
        size_hint: .2, .2

    Button:
        text: "Ok"
        size_hint: .2, .2

    Button:
        text: "K"
        size_hint: .2, .2
