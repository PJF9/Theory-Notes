from tkinter import *
# Import everything for module


window = Tk() # Create a blank window
label = Label(window, text='This is too easy') # Create the rest
label.pack() # Display the window
window.mainloop() # This pevent the window for closing
