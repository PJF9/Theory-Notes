"""
To install a package we use:
    'pip install package'

To delete a package from our machine we use:
    'pip uninstall package'
    
To see which packages need upgrade, meaning that a latest version has come out, we use
the command:
    'pip list -o' | 'pip list --outdated'

And to upgrade a package we use:
    'pip package --upgrade'
    
Also to see which packages the package we want to install depended on we use:
    'pip show package'
"""
